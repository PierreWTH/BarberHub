document.addEventListener('DOMContentLoaded', () => {
    // Fonction pour liker
    constlikeElements = [].slice.call(document.querySelectorAll(a['data-action=like']))
    // Si on l'a bien récupéré
    if(likeElements) {

    }
})