{% extends 'base.html.twig' %}

{% block title %}Hello PersonnelController!{% endblock %}

{% block body %}





<table>
    <thead>
        <tr>
            <th> Pseudo </th>
            <th> Barbershop </th>
            <th> Statut </th>
        </tr>
    </thead>
    <tbody>
        <tr>
            {% for personne in personnel %}
            <td>{{ personne.user}}</td>
            <td>{{ personne.barbershop.nom}}</td>
            {% if personne.isManager == 1 %}
                <td> Manager </td>
            {% else %}
                <td> Employé </td>
            {% endif %}
            {% endfor %}
        </tr>
    </tbody>
</table>
    


{% endblock %}
