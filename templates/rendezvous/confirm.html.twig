{% extends 'base.html.twig' %}

{% block title %} Mon espace {% endblock %}

{% block body %}

<section id="confirm-rdv-container">
    <div class="confirm-container">
        <div class="confirm-image">
            <img src="{{lastRDV.personnel.barbershop.photo}}" alt="Votre image" />
            <div class="image-overlay"></div>
            <div class="image-text">
                <p> Merci !</p>
            </div>
        </div>
        <div class="confirm-rdv">
            <div class="confirm-text">
                <p>Votre rendez-vous à bien été pris en compte. Vous allez recevoir un email de confirmation. 
                Vous pourrez retrouver les détails de votre rendez-vous dans votre espace. </p>
            </div>
            <div class="recap-rdv">
                <h2>Votre rendez vous chez {{ lastRDV.personnel.barbershop.nom}} :</h2>

                <p><span class="uppercase">Date</span> : {{lastRDV.debut|date("d/m/Y")}}</p>
                <p><span class="uppercase">Heure</span> : {{lastRDV.debut|date("H:i")}} - {{lastRDV.fin | date("H:i")}}</p>
                {% for prestation in lastRDV.barberprestation %}
                    <p><span class="uppercase">Prestation</span> : {{prestation.prestation.nom}} • {{prestation.prix}}€</p>
                {% endfor %}

                <p><span class="uppercase">Avec</span> : {{lastRDV.personnel.user.pseudo}}</p>

            </div>
            <div class="confirm-links">
            <a href="{{path('app_home')}}" class="rdv-back-to-home">Revenir à l'accueil</a> <a href="{{path('app_myspace')}} " class="rdv-back-to-home grey">Voir mes rendez-vous</a>
            </div>
        </div>
    </div>
</section>

{% endblock %}