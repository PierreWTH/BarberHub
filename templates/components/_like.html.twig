{% if app.user %}  
<a href="{{ path('like_barbershop', {id: barbershop.id}) }}" data-action = "like">
{% endif %}

<span>{{barbershop.likes|length }} J'aime </span>

{% if app.user %}
</a>
{% endif %}

<script src="{{ asset('js/like.js')}}"></script>